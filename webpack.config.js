const paths = require("./paths");

module.exports = {
  resolve: {
    // 配置模块扩展名
    extensions: [".js", ".ts", ".less", ".tsx"],
    // 配置模块别名或目录别名
    // alias: {
    //   src: paths.appSrc,
    // },
  },
};
